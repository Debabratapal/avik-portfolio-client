import React from 'react';
import { Header } from '../Header/Header';
import SliderImage from '../SliderImage/SliderImage';

export const UploadPage = () => {
  return [
  <Header key={0} title={'Admin Dashboard'} />,
  <SliderImage key={1} />
  ]
}